#include <iostream>
#include <vector>
#include <cmath>

using namespace std;

int main(void){
	int h;
	int v[pow(2, 21)];
	int sumV = 0;

	cin >> h;
	
	int numV = pow(2, h+1) - 1;
	numV[1] = 0;
	for(int i=2; i <= numV; i++){
		scanf("%d", &a[i]);
		sumV += a[i];
	}

	for(int i=
	return 0;
}
